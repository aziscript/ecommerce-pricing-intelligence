ecommerce_pricing:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      dbname: ecommerce_platform
      user: postgres
      password: postgres123
      schema: ecommerce
      threads: 4
      connect_timeout: 10
