name: ecommerce_pricing
version: "1.0.0"
config-version: 2

profile: ecommerce_pricing

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_pricing:
    staging:
      +materialized: view
      +schema: ecommerce
    marts:
      +materialized: table
      +schema: ecommerce
